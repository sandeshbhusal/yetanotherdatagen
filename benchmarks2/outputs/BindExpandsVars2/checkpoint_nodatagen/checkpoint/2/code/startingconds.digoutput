vtrace1 (11 invs):
1. MAXDATA + cp1_off - n1 - n2 == 0
2. -cp1_off**2 + cp1_off*n1 + cp1_off - n1 == 0
3. -cp1_off <= 0
4. max(n1, n2) - MAXDATA == 0
5. min(n1, n2) - cp1_off == 0
6. MAXDATA - max(n1, n2, 0) == 0
7. min(MAXDATA, n1, n2) - cp1_off == 0
8. MAXDATA - max(cp1_off, n1, n2) == 0
9. max(cp1_off, n1, n2, 0) - MAXDATA == 0
10. cp1_off - n1 === 0 (mod 2035)
11. -MAXDATA + n2 === 0 (mod 2035)
